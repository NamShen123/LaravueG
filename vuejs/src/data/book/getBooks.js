import { api } from '../../helper/api.js'

export async function getBooks () {
    // try {
    //     api.attachToken();
    //     api.setHeaderCommon();
    //     const response = await axios.get('http://localhost:8000/api/books');
    //     return response;
    // } catch (error) {
    //     return error;
    // }

    // TODO: tham khao to chuc code
}